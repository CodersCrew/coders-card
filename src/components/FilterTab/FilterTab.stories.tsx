import React from 'react';

import { FilterTab } from './FilterTab';

export default {
  title: 'FilterTab',
  component: FilterTab,
};

export const BaseFilterTab = () => {
  return <FilterTab />;
};
